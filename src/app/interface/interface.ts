export interface Product {
    id: number,
    name: string,
    price: number,
    path: string,
    inStock: number,
    best: boolean,
    color: string,
    categories: string,
    size: string[],
}
